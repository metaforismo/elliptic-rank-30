# Open subproblems

1. Recover the executable second rank-17 K3 fibration used for the current
   rank-29 record.
2. Find a split `E8` rational elliptic surface and a branch triple whose three
   twist ranks sum to at least 22 over `Q(t)`.
3. Lift a high-rank finite-field packet with explicit rational sections.
4. Solve a 31-zero norm identity over `Q` and certify a rank-30 span.
5. Determine the exact rational ranks of the marked cyclic `j=0` invariant and
   anti-invariant surfaces.
6. Produce an explicit Level-4 candidate and run the full two-system audit.
